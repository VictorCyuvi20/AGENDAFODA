Create database dbagenda;

use dbagenda; 

Create table tbUsuarios (
	nome VARCHAR(60) NOT NULL,
	usuario VARCHAR(20) NOT NULL primary key,
    telefoone VARCHAR(15),
    senha Varchar(15) NOT NULL
    );
    
    